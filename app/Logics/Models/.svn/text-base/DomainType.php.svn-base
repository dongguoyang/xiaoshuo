<?php

namespace App\Logics\Models;

class DomainType extends Base
{
    protected $table = 'domain_types';

    protected $fillable = ['name', 'status', 'rand_num', 'pre', 'ip_num', 'time_len', 'no', 'view_num', ];

    public $fields = [
        'id',
        'name', 'status', 'rand_num', 'pre', 'ip_num', 'time_len', 'no', 'view_num',
    ];

}